# License Header Policy

Cypra is MIT licensed repository-wide. The server, dashboard, CLI, examples,
and SDKs inherit the root `LICENSE` unless a file explicitly states otherwise.

Source files do not require per-file license headers. Generated artifacts must
preserve upstream notices when their tools emit them.
