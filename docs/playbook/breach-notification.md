# Breach Notification

Use audit NDJSON exports to determine impact. Preserve append-only audit rows, rotate secrets, notify affected tenants and downstream OIDC clients, and document all actions in the operator incident log.
